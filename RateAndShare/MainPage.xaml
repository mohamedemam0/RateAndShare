<Page
    x:Class="RateAndShare.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:RateAndShare"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
    </Grid>

    <Page.BottomAppBar>
        <CommandBar>
            <AppBarButton x:Name="RateBtn"
                          Label="Rate" Icon="Favorite"
                          Click="RateBtn_Click"/>
            <AppBarButton x:Name="shareBtn"
                          Icon="ReShare" Label="Share"
                          Click="shareBtn_Click"/>
            <CommandBar.SecondaryCommands>
                <AppBarButton x:Name="moreAppsBtn" 
                              Label="more apps" 
                              Click="moreAppsBtn_Click"/>
                <AppBarButton x:Name="detailsBtn"
                              Label="app details"
                              Click="detailsBtn_Click"/>
            </CommandBar.SecondaryCommands>
        </CommandBar>
    </Page.BottomAppBar>

</Page>
